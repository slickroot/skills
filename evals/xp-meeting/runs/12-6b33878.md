```
run:        12
date:       2026-07-29
prompt:     /xp-meeting I want this repo to be app/ server/ not have server/ inside our expo app
SKILL.md:   6b33878
log:        ~/.claude/projects/-Users-slickroot-work-elaich-thelongcompound/9c222779-18db-4a6d-8761-c71494e73c83.jsonl
```

Two issues filed: #156 "Delete what I no longer use", #157 "Put the Expo app in `app/`".
Thirteen questions. Second run of `6b33878`.

| id | verdict | evidence |
|---|---|---|
| `ONE-1` | **fail** | Story text, the shape run 11 pre-registered an edit for. At t75, cleaning up before the stories: "The `supabase/migrations/0008_session.sql` comment also mentions `session-lab.ts`, but migrations are history and **I'd leave it alone**" — the exact phrasing the check names as an announcement, and it lands as a criterion in story 1: "`supabase/migrations/0008_session.sql` is left untouched." No turn behind it. The sibling repair in the same breath — the stale `prompt-lab/` reference in `server/src/prompts.ts` — *is* exempt under the same-fact clause, since deleting `prompt-lab/` is the fact and you approved it at t35; the migration comment is the same fact decided the other way, and that direction was never yours. Two smaller ones, both inside a turn asking about something else: "I wouldn't create an `app/README.md`" in Q8's suggestion (t44), and "(`.claude/` and `.agents/` I'm treating as obviously repo-level — they stay at root, no question needed.)" at t47, while `supabase/`, `scripts/`, the prompt labs, `CONTRIBUTING.md`, `README.md`, `prototypes/`, `.env` and `.devrun.env` each got their own question |
| `HANDS-1` | **fail** | Story 2's proof line ends "and the app launches on the simulator". t65 says whose hands: "plus **you** launching the app once on the simulator" — and nothing else in the story can prove the move worked, since t65 also names Metro as the thing the three commands don't exercise. No `task` issue; t75 states the opposite out loud before the go-ahead: "no slice here needs anything from you outside the code, so there's no `task` issue". The `task` label exists — t16 confirmed it — so nothing blocked filing one. Graded on substance: the check's wording says "never a *slice* inside a story" and this arrived as an acceptance criterion instead. See notes |
| `CLOSED-1` | **pass** | Fifth running. `app/` enumerated file by file down to `expo-env.d.ts` and the four app-owned scripts; the root `package.json` pinned to `private`, no workspaces, `db:*` scripts and the `supabase` devDependency and nothing else; root `scripts/` given its two files by name; `git mv` named as the mechanism so history follows; the three ignore files listed. Story 1 names every deletion path including the `lab:report` npm script and the `.claude/settings.local.json` permissions |
| `SHOW-1` | **pass** | Fifth running. Both stories in full at t75, "file them" at t76, two `gh issue create` in one call at t78. Diffed both bodies off GitHub against t75: word for word. The agent's own check at t81 compared body *lengths*, not text — the word-for-word verification here is the grader's |
| `TOUCH-1` | **pass** | Fifth running. Two creates, no labels invented (t83 says so and offers to add one on request), two heredocs into `$CLAUDE_JOB_DIR/tmp`. Everything else is `ls`, `cat`, `grep`, `git ls-files`, `gh label list`. No file, branch or config touched — t83: "No code was touched in this session; the repo is exactly as you left it" |

Three passes, two fails.

**`ONE-1` failed on exactly the shape run 11 predicted.** Run 11's closing line was: *if run 12 fails `ONE-1` on story text again — something in a slice that traces to no answer of yours — the edit is waiting.* That is what happened, and it happened in the cleanup turn just before the stories, which is the turn where the agent stops asking and starts writing. Both t44 and t47 are the same reflex a few turns earlier: it found something nobody had ruled on, and ruled on it in passing rather than spending a turn.

**The same-fact exemption did its job this run.** Added before run 11 and immediately suspected of being too generous, it cleanly separated two repairs sitting in the same sentence at t75: `prompts.ts` exempt, the migration comment not. That is the line the exemption was meant to draw.

**`HANDS-1` is a fail the check can only just reach.** The manual step arrived as an acceptance criterion, not a slice, and the check's wording only forbids slices. The substance is the check's whole point — a story that cannot be called done without your hands, and no `task` issue — so it is graded fail, with the wording gap recorded below rather than patched mid-run.

## Notes

- **`HANDS-1` says "slice" where it means "anything in a story".** Run 12 is the first run to put a hands-only step somewhere other than a slice. Reword before run 14 — *any step I have to run myself ... is its own issue labelled `task`, never anywhere inside a story* — or the next verdict of this shape will hinge on where the sentence landed rather than on what it asks of you.

- **Issue titles are written by the agent and never put in front of you.** #156 and #157 got their titles from the story headings at t75, so you did see the words — but as headings, not as the issue's title, and no turn asked. Still unchecked, and run 13 shows the same reflex with no heading to cover it.

- **Filed unlabelled again.** Seventh run. t83 volunteers the gap and offers `enhancement` rather than inventing one, which is `TOUCH-1` behaving. Run 11 said write the check or stop noting it; this is the last time it goes in a notes section.

- **The homework caught a real error before the stories, not after.** t72 grepped the deletions for live references and t75 corrected its own plan: `scenarios/example.json` is seed data read by `scripts/seed-cloud.ts`, not lab data, so it stays. That correction reached you before the go-ahead, which is where corrections belong.

## The one edit

**Not applied — you asked for no skill edit this run.** Recorded so run 14 can still use it.

The evidence points where run 11 said it would: *the story holds only what I answered; if writing it turns up something I wasn't asked, that's another question, not another bullet.* Aimed at t75, and at t44 and t47 by the same logic. Run 13 passed `ONE-1` on clean evidence with this rule absent, so the case for it now rests on run 12 alone.
